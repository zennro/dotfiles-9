# -*- mode: conf; -*-
[user]
  name = Colin Bell
  email = col@baibell.org
[color]
  ui = true
[alias]
  br = branch
  cancel = reset –soft HEAD^
  ci = commit
  co = checkout
  gr = log --decorate --graph --pretty=oneline --abbrev-commit
  la = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  lgall = log --all --graph  --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  llog = log --date=local
  oneline = log --pretty=oneline
  slog = shortlog -s -n
  st = status -sb
  #unpushed = log ORIG_HEAD..HEAD --stat --no-merges
  unpushed = log --branches --not --remotes
  type = cat-file -t
  print = cat-file -p
[push]
  default = tracking
[http]
  sslVerify = false
[diff]
  # For Github-style word diffs
  wordRegex = [^[:space:]]
